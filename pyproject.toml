[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "dbgit"
version = "0.1.0"
description = "SQL Server 프로시저·함수·공통코드 환경 비교 도구"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
